# Optional: use full API URL when frontend and API are on different hosts
# VITE_API_URL=https://your-api-domain.com/api

# Optional: when using Vite proxy, set backend origin here
VITE_PROXY_TARGET=http://localhost:5001
